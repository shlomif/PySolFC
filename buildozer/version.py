#!/usr/bin/env python3
# ---------------------------------------------------------------------------

import sys
sys.path.insert(0, str(".."))

from pysollib.settings import VERSION
print ("VERSION =",VERSION)
